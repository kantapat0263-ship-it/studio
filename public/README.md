This `public` directory is for static assets that don't need to be processed by the build system.

You can place images, fonts, or other files here, and they will be served from the root of your site.

For example, if you place a file named `logo.png` in this folder, you can access it in your code with `<Image src="/logo.png" ... />`.

Please upload your `logo.png` file here.
